export type TaskType = {
    id: string;
    title: string;
    description: string;
    due_date: string;
    status: string;
}

export type TaskListType = {
    title: string;
    description: string;
    due_date: string;
    status: string;
}